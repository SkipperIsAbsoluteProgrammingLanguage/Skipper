fn add(int x) -> int {
    x = x;
    x = x;
    x = x + 1;
    x = x;
    x;
    x = x;
    return x;
}

fn main() {
    int x = 5;
    for (int i = 0; i < 40000; i++) {
        x = add(x);
    }
    println(x);
}
