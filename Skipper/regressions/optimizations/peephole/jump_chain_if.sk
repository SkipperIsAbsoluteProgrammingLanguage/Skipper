fn main() {
    int x = 0;

    if (true) {
        // всегда сюда
        if (true) {
            // вторая ветка
            x = x + 1;
        } else {
            x = x + 100;
        }
    } else {
        x = x + 1000;
    }

    println(x);
}