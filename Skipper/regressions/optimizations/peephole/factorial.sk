fn factorial(long n) -> long {
    if (n <= 1) {
        return 1;
    }
    return n * factorial(n - 1);
}

fn main() {
    long n = 15;
    long r = factorial(n);
    println("factorial(" + n + ") = " + r);
}