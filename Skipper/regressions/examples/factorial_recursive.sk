fn factorial(long n) -> long {
    if (n <= 1) {
        return 1;
    }

    return n * factorial(n - 1);
}

fn main() {
    // warm
    for (int i = 0; i < 4; i++) {
        factorial(18);
    }

    long n = 20;
    long result = factorial(n);
    println("factorial(" + n + ") = " + result);
}
