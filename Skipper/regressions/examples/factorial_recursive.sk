fn factorial(long n) -> long {
    if (n <= 1) {
        return 1;
    }

    return n * factorial(n - 1);
}

fn main() {
    long n = 20;
    long result = factorial(n);
    println("factorial(" + n + ") = " + result);
}
