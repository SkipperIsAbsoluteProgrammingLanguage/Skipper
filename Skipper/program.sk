class A {
    int x;
    bool b;
}

fn main() -> int {
    A a;
    int b = 2;
    int start = time();
    for (int i = 0; i < 3; i = i + 1) {
        b = b + 1;
        if (b >= 4) {
            print(b);
        }
    }
    int end = time();
    print("App time:");
    print(end - start);
    return 1;
}